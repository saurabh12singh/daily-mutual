version: 0.2

phases:
  build:
    commands:
      - echo Build started
      - mvn clean package
  post_build:
    commands:
      - echo Build completed
artifacts:
  files:
    - target/MutualFund-service.jar
